[package]
name = "data"
version = "0.1.0"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive", "rc"] }
rmp-serde = "1.1"
strum = { version = "0.25", features = ["derive"] }
